query UserByID($id: ID!) {
  user(by: { id: $id }) {
    ...UserParts
    createdAt
    updatedAt
    customer {
      ...CustomerParts
    }
    identities(first: 100) {
      edges {
        node {
          ...IdentityParts
        }
      }
    }
  }
}
