query ProductByName($name: String!) {
  product(by: { name: $name }) {
    ...ProductParts
    createdAt
    updatedAt
    features(first: 100) {
      edges {
        node {
          ...FeatureParts
        }
      }
    }
  }
}
