query ProductById($id: ID!) {
  product(by: { id: $id }) {
    ...ProductParts
    createdAt
    updatedAt
    features(first: 100) {
      edges {
        node {
          ...FeatureParts
        }
      }
    }
  }
}
