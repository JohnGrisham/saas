query CustomerByID($id: ID!) {
  customer(by: { id: $id }) {
    ...CustomerParts
    createdAt
    updatedAt
    user {
      ...UserParts
    }
    subscriptions(first: 100) {
      edges {
        node {
          ...SubscriptionParts
        }
      }
    }
  }
}
