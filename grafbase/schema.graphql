enum IdentityType {
  CREDENTIALS
  GITHUB
}

type User @model {
  id: ID!
  name: String
  email: String! @unique
  identities: [Identity!]!
}

type Identity @model {
  id: ID!
  type: IdentityType!
  password: String
  user: User!
}

type UserList @model {
  id: ID!
  users: [User!]!
}

type IdentityList @model {
  id: ID!
  identities: [Identity!]!
}
